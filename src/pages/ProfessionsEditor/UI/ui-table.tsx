import {Box} from "@mui/material";
import {BoxProps} from "@mui/material/Box/Box";

interface IUIProfessionsTableProps extends BoxProps {

}


export default function UIProfessionsTable({...props}: IUIProfessionsTableProps) {
    return (
        <Box {...props}>
            
        </Box>
    )
}